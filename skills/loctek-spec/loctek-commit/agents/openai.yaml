interface:
  display_name: "Loctek Commit"
  short_description: "Commit changes with intent records"
  default_prompt: "Use $loctek-commit to commit my current safe related changes, generate a Loctek intent, and create a PR draft."
